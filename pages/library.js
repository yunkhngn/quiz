import React from 'react'
import { Template } from "@/components/template";
import { urlMeta } from "@/components/lib";

const library = () => {
  return (
    <Template urlMeta={urlMeta.library}>
        Thư viện
    </Template>
  )
}

export default library