import React from "react";
import { Template } from "@/components/template";
import { urlMeta } from "@/components/lib";

const learning = () => {
  return (
    <Template urlMeta={urlMeta.learning}>
        Learning
    </Template>
  );
};

export default learning;
