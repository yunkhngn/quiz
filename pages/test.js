import React from 'react'
import { Template } from "@/components/template";
import { urlMeta } from "@/components/lib";

const test = () => {
  return (
    <Template urlMeta={urlMeta.test}>
        Learning
    </Template>
  )
}

export default test